name: di
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.6 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  build_runner: ^2.3.0
  injectable_generator: ^2.1.3
  lints: ^2.0.0
  test: ^1.16.0

publish_to: none

dependencies:
  get_it: ^7.2.0
  injectable: ^2.1.0

  counter: 
    path: '../counter'
    
  counter_storage: 
    path: '../counter_storage'

  logger: 
    path: '../logger'
